name: chat_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.0.2
  firebase_messaging: ^15.0.3
  firebase_storage: ^12.0.1

  # State management
  provider: ^6.1.2

  # other
  cupertino_icons: ^1.0.6
  image_picker: ^1.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  #flutter_launcher_icons: ^0.13.0

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

#
#  flutter_icons:
#    android: "launcher_icon"
#    ios: true
#    image_path: "assets/icon/sms.png"
